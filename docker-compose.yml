services:
  carbon-shade-web:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    # Development: Vite dev server with HMR. For production, use Dockerfile directly.
    command: npm run dev
